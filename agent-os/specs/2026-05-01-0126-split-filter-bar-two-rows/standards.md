# Standards for Split FilterBar into two rows

No standards from `agent-os/standards/index.yml` apply directly to this work. The standards index is focused on KMP / Kotlin / mesh / cloud architecture; the `argus-webui` module is a separate vanilla TypeScript + Tailwind frontend with no UI standards indexed there.

The only loosely related entry is `workflow/commit-conventions` (commit types, imperative subject, no agent-attribution trailers); this applies at commit time and is reinforced by the repo memory entry "No AI attribution in commits."
